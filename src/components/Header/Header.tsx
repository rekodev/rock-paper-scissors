import ScoreBoard from '../ScoreBoard';
import { StyledHeader } from './style';

const Header = () => {
  return (
    <StyledHeader>
      <ScoreBoard />
    </StyledHeader>
  );
};

export default Header;
